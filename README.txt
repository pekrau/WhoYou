WhoYou: Simple accounts database for web applications.

A database containing accounts and teams. The passwords are stored as
hashes using a salt to be defined upon installation.

The database is editable via a RESTful web interface.

Simple functions are provided to support account and team data retrieval,
and account authorization, which can then be used for other web applications
on the same server.
